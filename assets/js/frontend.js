// Frontend JavaScript for Strike Lightning Payment
(function($) {
    'use strict';
    
    // This file is included but the main functionality is in the receipt page inline script
    // to avoid complexity and ensure it works properly
    
    console.log('Strike Lightning Payment frontend script loaded');
    
    // Add debugging for AJAX calls
    if (typeof strike_lightning_ajax !== 'undefined') {
        console.log('Strike Lightning AJAX config:', strike_lightning_ajax);
    }
    
})(jQuery);
